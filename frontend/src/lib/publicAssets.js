/** URLs for files in /public (do not import from public/ — use paths + BASE_URL). */
const B = import.meta.env.BASE_URL || '/';

/** Full brand mark — nav / sidebar / landing header (see index.html for tab favicon). */
export const meritNookLogoUrl = `${B}images/merit-nook-logo.png`;
